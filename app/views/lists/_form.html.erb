<%= simple_form_for(@list) do |f| %>

  <div class="split-fields">
    <div class="field">
        <%= f.input :name, :placeholder => "Name", :label => false %>
    </div>
    <div class="button">
        <%= f.button :submit, "Add List" %>
    </div>  
  </div>
<% end %>